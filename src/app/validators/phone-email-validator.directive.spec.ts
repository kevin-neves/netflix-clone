import { PhoneEmailValidatorDirective } from './phone-email-validator.directive';

describe('PhoneEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
